server:
  port: ${PORT:8010}

spring:
  application:
    name: config-server
  #By default it is git only
  profiles:
    active: native, git

#Symmetric encryption
encrypt:
  key: sdfsW234sdDSF234hksdfddwdf34FDFd3DFdfj4Fdm